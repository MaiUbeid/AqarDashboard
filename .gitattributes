# Laravel git normalization

# Define binary file attributes.
# - Do not treat them as text.
# - Include binary diff in patches instead of "binary files differ."
*.gif     -text diff
*.gz      -text diff
*.ico     -text diff
*.jpg     -text diff
*.png     -text diff
*.phar    -text diff
*.exe     -text diff
*.ttf     -text diff

*.php       eol=lf
*.vue       eol=lf
*.js       eol=lf
